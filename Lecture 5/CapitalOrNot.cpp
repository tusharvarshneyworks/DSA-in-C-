#include<iostream>
using namespace std;
int main(){
    char ch;
    cout<<"Enter a Alphabet: ";
    cin>>ch;
    if((int)ch>=65 && (int)ch<=90) cout<<"It is a Capital Alphabet";
    else cout<<"It is not a Capital Alphabet";
    
    // if(ch>=65 && ch<=90) cout<<"It is a Capital Alphabet";
    // else cout<<"It is not a Capital Alphabet";
    //aise bhi likh skte
}