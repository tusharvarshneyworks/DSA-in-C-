#include <iostream>
using namespace std;
int main(){

    int num;
    cout << "Enter a Positive Integer: ";
    cin >> num;
    if (num % 2 == 0) cout << "Number is Even";     //ye juth hua to kuch nahi chalega
    if (num % 2 == 1) cout << "Number is odd";
    //do bar check
}